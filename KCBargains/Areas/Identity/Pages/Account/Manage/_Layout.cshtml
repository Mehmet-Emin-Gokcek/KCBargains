@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container w-75" style="padding-top:20px;">
    <section class="page-section" id="about">
        <div class="text-center">
            <h2 class="section-heading text-uppercase">Manage your account</h2>
            <h3 class="section-subheading text-muted mb-5">You may change your account settings here</h3>
        </div>
                    <div class="row">
                        <div class="col-md-3">
                            @await Html.PartialAsync("~/Areas/Identity/Pages/Account/Manage/_ManageNav.cshtml")
                        </div>
                        <div class="col-md-9">
                            @RenderBody()
                        </div>
                    </div>
           
</section>
</div>
        @section Scripts {
            @RenderSection("Scripts", required: false)
        }
